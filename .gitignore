build/
build-*/
.cache/
*.user
*.user.*
CMakeLists.txt.user

# Editors
.vscode/
.idea/
*.swp

# Bundled font packages — fetched by scripts/fetch-fonts.py, not committed
fonts/

# Emoji source font (dropped in by hand) and the stickers extracted from it at
# build time by scripts/extract-stickers.py — neither is committed
emoji-fonts/
resources/stickers/

# Whisper ONNX models (dropped in by hand) for auto subtitle generation — large, not committed
models/

# Sample media (kept out of the repo)
*.mp4
*.mov
*.mkv
*.webm
*.wav
*.mp3

# OS
.DS_Store
